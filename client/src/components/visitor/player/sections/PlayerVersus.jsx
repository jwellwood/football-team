import React from 'react';
// Internal
import VersusWrapper from 'components/ui/lists/VersusWrapper';

const PlayerVersus = ({ data }) => {
  return <VersusWrapper data={data} />;
};

export default PlayerVersus;
